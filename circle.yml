node:
  verson: 7.9.0
dependencies:
  pre:
    - pip install awsebcli
    - npm install
test:
  override:
    - echo "done"
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - npm run pack
      - eb deploy LearningObject-Service-Production --profile default